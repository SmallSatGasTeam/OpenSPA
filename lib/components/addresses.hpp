#ifndef COMP_ADDRSSES_HPP
#define COMP_ADDRSSES_HPP

#include "../logical_address.hpp"

const LogicalAddress ADDRESS_AEROBOOM(0,1);
const LogicalAddress ADDRESS_DATASENSORS(0,2);
const LogicalAddress ADDRESS_FILTER(0,3);
const LogicalAddress ADDRESS_LOGGING(0,4);

#endif
